# Letter-Recognition

This repository contains the architecture, dataset processing, and training logic for a custom Convolutional Neural Network (CNN) built to classify and recognize image-based letters.
The trained model produced by this repository is actively deployed and served via a Flask web application. You can view the front-end implementation and API routing in the companion repository here:https://github.com/Sujinthan/Digitize
